{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dev-mcp-bridge",
  "description": "Development-only capability for MCP Bridge debugging",
  "context": "local",
  "windows": ["main"],
  "permissions": [
    "mcp-bridge:default"
  ]
}
